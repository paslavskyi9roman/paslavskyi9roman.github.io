export default function LoginPage() {
  return (
    <section className="panel max-w-xl p-6">
      <h1 className="text-2xl font-semibold">Login (MVP placeholder)</h1>
      <p className="mt-2 text-slate-300">
        Supabase Auth wiring comes next. For now this screen marks where email/social login will live.
      </p>
    </section>
  );
}
